{
  "globalHeaders": {
    "Access-Control-Allow-Origin": "*",  // Allows all domains; consider specifying only required domains.
    "Access-Control-Allow-Methods": "GET, POST, PUT, DELETE, OPTIONS",
    "Access-Control-Allow-Headers": "Content-Type, Authorization"
  },
  "routes": [
    {
      "route": "/test.js",
      "headers": {
        "Access-Control-Allow-Origin": "*" // Adjust this as necessary for your use case
      }
    }
  ]
}
